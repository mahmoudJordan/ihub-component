{
    "configurations": [
        {
            "address": "localhost",
            "localRoot": "${workspaceFolder}",
            "name": "Attach to Remote",
            "port": 9229,
            "timeout": 999999999,
            "remoteRoot": "/usr/src/app",
            "request": "attach",
            "type": "node",
            "restart": true,
            "preLaunchTask": "setup_debugging_session"
        }
    ]
}